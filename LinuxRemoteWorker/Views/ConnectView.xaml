<UserControl x:Class="LinuxRemoteWorker.Views.ConnectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:LinuxRemoteWorker.Converters">
    <UserControl.Resources>
        <conv:InvertBoolConverter x:Key="InvertBoolConverter"/>
        <conv:ErrorColorConverter x:Key="ErrorColorConverter"/>
        <conv:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>

        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#888"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0 0 0 4"/>
        </Style>
        <Style x:Key="Field" TargetType="TextBox">
            <Setter Property="Background" Value="#1E1E2E"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#2A2A3E"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10 8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="CaretBrush" Value="White"/>
        </Style>
        <Style x:Key="ProfileItemStyle" TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#AAA"/>
            <Setter Property="Padding" Value="10 8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Bg" Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#1E1E2E"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#2A2A4E"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Saved profiles panel -->
        <Border Grid.Column="0" Background="#0A0A15" BorderBrush="#2A2A3E" BorderThickness="0 0 1 0">
            <DockPanel>
                <Border DockPanel.Dock="Top" Padding="16 16 16 8">
                    <TextBlock Text="SAVED SERVERS" Foreground="#444" FontSize="10" FontWeight="Bold"/>
                </Border>

                <Border DockPanel.Dock="Bottom" Padding="12 8" BorderBrush="#2A2A3E" BorderThickness="0 1 0 0">
                    <Button Content="🗑 Delete" Command="{Binding DeleteProfileCommand}"
                            Background="Transparent" BorderThickness="0" Foreground="#666"
                            FontSize="12" Cursor="Hand" HorizontalAlignment="Left"
                            IsEnabled="{Binding SelectedProfile, Converter={StaticResource BoolToVisibilityConverter}}"/>
                </Border>

                <ListBox ItemsSource="{Binding Profiles}"
                         SelectedItem="{Binding SelectedProfile}"
                         Background="Transparent" BorderThickness="0"
                         Padding="8 0"
                         ItemContainerStyle="{StaticResource ProfileItemStyle}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock Text="{Binding Name}" FontSize="13"/>
                                <TextBlock Text="{Binding Host}" Foreground="#555" FontSize="11"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </Border>

        <!-- Connection form -->
        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Width="400" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0 40">
                <TextBlock Text="SSH Connection" Foreground="White" FontSize="24" FontWeight="Bold" Margin="0 0 0 8"/>
                <TextBlock Text="Connect to your Linux server using an SSH private key" Foreground="#666" FontSize="13" Margin="0 0 0 32"/>

                <TextBlock Text="PROFILE NAME" Style="{StaticResource FieldLabel}"/>
                <TextBox Text="{Binding ProfileName, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource Field}" Margin="0 0 0 16"/>

                <TextBlock Text="HOST / IP" Style="{StaticResource FieldLabel}"/>
                <TextBox Text="{Binding Host, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource Field}" Margin="0 0 0 16"/>

                <TextBlock Text="USERNAME" Style="{StaticResource FieldLabel}"/>
                <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource Field}" Margin="0 0 0 16"/>

                <TextBlock Text="PRIVATE KEY PATH" Style="{StaticResource FieldLabel}"/>
                <Grid Margin="0 0 0 16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" Text="{Binding PrivateKeyPath, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource Field}"/>
                    <Button Grid.Column="1" Content="Browse" Command="{Binding BrowseKeyCommand}"
                            Margin="8 0 0 0" Padding="12 8" Background="#2A2A3E" Foreground="#AAA"
                            BorderThickness="0" FontSize="13" Cursor="Hand"/>
                </Grid>

                <TextBlock Text="PASSPHRASE (optional)" Style="{StaticResource FieldLabel}"/>
                <PasswordBox x:Name="PassBox" Margin="0 0 0 24"
                             Background="#1E1E2E" Foreground="White" BorderBrush="#2A2A3E"
                             BorderThickness="1" Padding="10 8" FontSize="14"/>

                <!-- Buttons -->
                <Grid Margin="0 0 0 8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Content="Connect" Command="{Binding ConnectCommand}"
                            IsEnabled="{Binding IsBusy, Converter={StaticResource InvertBoolConverter}}"
                            Padding="0 14" Background="#3D5AFE" Foreground="White" BorderThickness="0"
                            FontSize="15" FontWeight="Medium" Cursor="Hand" Margin="0 0 8 0"/>
                    <Button Grid.Column="1" Content="💾 Save" Command="{Binding SaveProfileCommand}"
                            Padding="16 14" Background="#2A2A3E" Foreground="#AAA" BorderThickness="0"
                            FontSize="14" Cursor="Hand"/>
                </Grid>

                <!-- Status -->
                <TextBlock Text="{Binding StatusMessage}" Margin="0 12 0 0" TextAlignment="Center" FontSize="13"
                           Foreground="{Binding HasError, Converter={StaticResource ErrorColorConverter}}"
                           Visibility="{Binding StatusMessage, Converter={StaticResource BoolToVisibilityConverter}}"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
